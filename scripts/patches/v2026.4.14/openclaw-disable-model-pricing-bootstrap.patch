--- a/src/gateway/server-runtime-services.ts
+++ b/src/gateway/server-runtime-services.ts
@@ -86,9 +86,13 @@
   return {
     heartbeatRunner: createNoopHeartbeatRunner(),
     channelHealthMonitor,
+    // Popiai patch: disable model-pricing bootstrap to avoid startup delays.
+    // The gateway fetches https://openrouter.ai/api/v1/models on startup which
+    // times out (15s) in regions with slow external API access, delaying the
+    // WebSocket handshake.  See openclaw/openclaw#60116, openclaw/openclaw#59348.
     stopModelPricingRefresh:
-      !params.minimalTestGateway && process.env.VITEST !== "1"
-        ? startGatewayModelPricingRefresh({ config: params.cfgAtStart })
+      !params.minimalTestGateway && process.env.VITEST !== "1" && process.env.OPENCLAW_SKIP_MODEL_PRICING !== "1"
+        ? startGatewayModelPricingRefresh({ config: params.cfgAtStart })
         : () => {},
   };
 }
