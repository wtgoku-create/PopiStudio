diff --git a/packages/gateway-protocol/src/schema/logs-chat.ts b/packages/gateway-protocol/src/schema/logs-chat.ts
--- a/packages/gateway-protocol/src/schema/logs-chat.ts
+++ b/packages/gateway-protocol/src/schema/logs-chat.ts
@@ -137,6 +137,7 @@ export const ChatFinalEventSchema = Type.Object(
     message: Type.Optional(Type.Unknown()),
     usage: Type.Optional(Type.Unknown()),
     stopReason: Type.Optional(Type.String()),
+    yielded: Type.Optional(Type.Literal(true)),
   },
   { additionalProperties: false },
 );
diff --git a/src/gateway/server-chat.ts b/src/gateway/server-chat.ts
--- a/src/gateway/server-chat.ts
+++ b/src/gateway/server-chat.ts
@@ -503,6 +503,16 @@ export function createAgentEventHandler({
       if (!isAborted) {
         const evtStopReason =
           typeof evt.data?.stopReason === "string" ? evt.data.stopReason : undefined;
+        const yieldedWaiting =
+          lifecyclePhase === "end" &&
+          evt.data?.yielded === true &&
+          evt.data?.livenessState === "paused" &&
+          evtStopReason === "end_turn" &&
+          evt.data?.aborted !== true &&
+          evt.data?.status !== "cancelled" &&
+          evt.data?.status !== "timed_out" &&
+          evt.data?.timeoutPhase == null &&
+          evt.data?.error == null;
         const evtErrorKind =
           readChatErrorKind(evt.data?.errorKind) ?? detectErrorKind(evt.data?.error);
         const evtErrorMetadata = extractSafeChatErrorMetadata(evt.data);
@@ -523,7 +533,11 @@ export function createAgentEventHandler({
               evtStopReason,
               evtErrorKind,
               evtErrorMetadata,
-              { agentId: finished.agentId, controlUiVisible: isControlUiVisible },
+              {
+                agentId: finished.agentId,
+                controlUiVisible: isControlUiVisible,
+                yielded: yieldedWaiting ? true : undefined,
+              },
             );
           }
         } else if (!(opts?.skipChatErrorFinal && lifecyclePhase === "error")) {
@@ -539,7 +553,11 @@ export function createAgentEventHandler({
             evtStopReason,
             evtErrorKind,
             evtErrorMetadata,
-            { agentId: sessionAgentId, controlUiVisible: isControlUiVisible },
+            {
+              agentId: sessionAgentId,
+              controlUiVisible: isControlUiVisible,
+              yielded: yieldedWaiting ? true : undefined,
+            },
           );
         }
       } else {
@@ -761,7 +779,7 @@ export function createAgentEventHandler({
     stopReason?: string,
     errorKind?: ErrorKind,
     errorMetadata?: Record<string, unknown>,
-    opts?: { agentId?: string; controlUiVisible?: boolean },
+    opts?: { agentId?: string; controlUiVisible?: boolean; yielded?: true },
   ) => {
     const { text, shouldSuppressSilent } = resolveBufferedChatTextState(clientRunId, sourceRunId, {
       suppressLeadFragments: false,
@@ -784,6 +802,7 @@ export function createAgentEventHandler({
         seq,
         state: "final" as const,
         ...(stopReason && { stopReason }),
+        ...(opts?.yielded ? { yielded: true as const } : {}),
         message:
           text && !shouldSuppressSilent
             ? {
diff --git a/src/gateway/server-chat.agent-events.test.ts b/src/gateway/server-chat.agent-events.test.ts
--- a/src/gateway/server-chat.agent-events.test.ts
+++ b/src/gateway/server-chat.agent-events.test.ts
@@ -2082,6 +2082,42 @@ describe("createAgentEventHandler", () => {
     expect(chatBroadcastCalls(broadcast)).toHaveLength(0);
   });
 
+  it("marks yielded final chat events so clients keep waiting for follow-up work", () => {
+    const { broadcast, chatRunState, handler } = createHarness({
+      resolveSessionKeyForRun: () => "agent:main:main",
+    });
+    chatRunState.registry.add("run-yielded", {
+      sessionKey: "agent:main:main",
+      clientRunId: "client-yielded",
+    });
+
+    handler({
+      runId: "run-yielded",
+      seq: 1,
+      stream: "assistant",
+      ts: Date.now(),
+      data: { text: "Waiting for registered continuation work." },
+    });
+    handler({
+      runId: "run-yielded",
+      seq: 2,
+      stream: "lifecycle",
+      ts: Date.now(),
+      data: { phase: "end", yielded: true, livenessState: "paused", stopReason: "end_turn" },
+    });
+
+    const payload = requireCall(
+      chatBroadcastCalls(broadcast).find(([, candidate]) => candidate.state === "final")?.[1],
+      "yielded final chat payload",
+    );
+    expect(payload).toMatchObject({
+      runId: "client-yielded",
+      sessionKey: "agent:main:main",
+      stopReason: "end_turn",
+      yielded: true,
+    });
+  });
+
   it("keeps live session setting metadata at the top level for lifecycle updates", () => {
     vi.mocked(loadGatewaySessionRow).mockReturnValue({
       key: "session-finished",
